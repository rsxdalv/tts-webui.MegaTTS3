############################################################
# Core runtime dependencies for MegaTTS3 as a library
# - Only modules actually imported by the codebase
# - Relaxed, semver-style ranges suitable for library usage
# - Platform/binary wheels like torch/numpy: keep major bounds
# Optional components are split into separate files:
#   requirements-ui.txt      -> Gradio web UI
#   requirements-align.txt   -> OpenAI Whisper aligner/frontend
############################################################

torch>=2.1,<3
numpy>=1.23,<2
librosa>=0.10
transformers>=4.41,<5
einops>=0.6
scipy>=1.10
pydub>=0.25,<1
pyloudnorm>=0.1,<1
WeTextProcessing>=1.0.4,<2
langdetect>=1.0.9
six>=1.16
PyYAML>=6.0
tqdm>=4.66
huggingface_hub>=0.24,<1

# Notes:
# - matplotlib is only used in plotting utils; install separately or via your own extra
# - torchaudio, modelscope, setproctitle, attrdict, x-transformers, torchdiffeq, httpx
#   are not imported anywhere in this repo and have been removed from core
# - openai-whisper and gradio are provided as optional extras in the files listed above
